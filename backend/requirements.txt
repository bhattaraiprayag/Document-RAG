# Core Framework
fastapi
uvicorn[standard]
python-multipart

# Environment & Config
python-dotenv
pydantic
pydantic-settings

# Vector Database
qdrant-client

# Document Processing
markitdown[all]

# Tokenization (for chunking engine)
# NOTE: transformers is used ONLY for AutoTokenizer, not for model inference
# Embedding/reranking is delegated to ml-api service via HTTP
transformers
tokenizers

# LLM Providers
openai
httpx

# NLP
nltk

# Testing
pytest
pytest-asyncio
pytest-cov
pytest-mock

# Code Quality
mypy
black
flake8
isort

# Observability (Optional)
langfuse
